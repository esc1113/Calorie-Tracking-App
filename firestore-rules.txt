// Add this block INSIDE your existing rules, alongside the habit tracker's
// match block — don't replace the whole file. Then click Publish.
//
// Full file will look something like:
//
// rules_version = '2';
// service cloud.firestore {
//   match /databases/{database}/documents {
//
//     ... your existing habit tracker rules ...
//
//     match /fuel_users/{uid}/{document=**} {
//       allow read, write: if request.auth != null && request.auth.uid == uid;
//     }
//   }
// }

match /fuel_users/{uid}/{document=**} {
  allow read, write: if request.auth != null && request.auth.uid == uid;
}
